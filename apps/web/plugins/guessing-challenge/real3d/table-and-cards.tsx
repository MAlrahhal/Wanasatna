'use client';

/**
 * Legacy table-and-cards module — giant center table and special cards removed.
 * Room geometry lives in lounge-room; self card lives in first-person-hands.
 * Kept as a thin re-export so any stale imports stay harmless.
 */
export { LoungeRoom } from './lounge-room';
